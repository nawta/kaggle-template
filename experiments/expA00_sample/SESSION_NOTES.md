# SESSION_NOTES: expA00_sample

<!-- Claude 実験のセッション記録テンプレート（記入例付き） -->

## セッション 1

- **日付**: 2026-02-10
- **目標**: テンプレートの動作確認

### 仮説

- サンプル実験が正常に動作することを確認する

### 試したアプローチと結果

| アプローチ | CV | LB | 備考 |
|-----------|----|----|------|
| デフォルト設定で実行 | - | - | テンプレート動作確認のため |

### ファイル構成

- `run.py`: メイン実行スクリプト（exp000_sample ベース）
- `config.yaml`: Hydra 設定
- `exp/000.yaml`: デフォルト設定（seed=0, folds=[0]）

### 重要な知見

- Claude 実験は `exp{A-Z}{NN}_` の命名規則に従う
- アルファベット部分は方針変更時にインクリメントする（A→B→C...）
- 番号部分は同一方針内の実験番号（00, 01, 02...）

### 次のステップ

- [ ] コンペ固有のデータ読み込み処理を追加
- [ ] ベースラインモデルの実装

### 性能変化の記録

| 変更内容 | CV (before) | CV (after) | 差分 |
|---------|------------|-----------|------|
| 初期ベースライン | - | - | - |

### コマンド履歴

```bash
uv run python -m experiments.expA00_sample.run
uv run python -m experiments.expA00_sample.run exp=000
```
